Space	Проект	Priority	Level	DoD	Ссылка	Action	Effort	ROI	Дата	
AI_Стратегия	Space AI_Стратегия как генштаб	CRITICAL	Core	Space работает по циклу Brief→Generate→Validate. Команда: Prompt Strategist, Domain Engineer, Optimization Analyst. Output: видение (3-5 строк), цели 3-6 мес с DoD, дорожная карта L2 (таблица: Проект|Статус|Ссылка|Action), архитектура high-level, правила работы с ИИ (Void центр, код через IDE, тесты через Judge), журнал решений (дата|решение|причина|где), промпты для других Space + action items. Проверка: после каждого approve → output соответствует структуре; Master/SPACE обновлены; decisions.md содержит запись; Void находит промпты в /docs/prompts/.	SPACE.xlsx	После каждого брифа: 1) проверить структуру output; 2) обновить Master/SPACE; 3) зафиксировать в decisions.md	2h	10x	05.02.2026	
AI_Стратегия	Цели 3-6 месяцев с DoD	CRITICAL	Core	Документ /docs/strategy.md содержит 5 целей: 1) Void + стек агентов (Ollama serve, 4 модели, system-промпты, тест CRUD+wb-fetcher+docker-compose); 2) ContextRAG HTML (таблица→HTML, публичные URL, промпты с ссылками); 3) Smart DB + PnL на VPS (Postgres 16+pgvector, wborders/finances/products, mv_daily_pnl, wb-fetcher ETL, backup, firewall); 4) TG-бот /pnl (aiogram 3.x, SELECT mv_daily_pnl, < 3s, заготовки /problem_sku/toxic); 5) Security VPS/Docker (/docs/security_checklist.md, UFW, SSH-keys, .env в .gitignore, тесты). Каждая цель с конкретным DoD. Проверка: /docs/strategy.md существует, содержит 5 разделов, DoD testable.	SPACE.xlsx, Master.xlsx	Создать /docs/strategy.md, прописать 5 целей с DoD, синхронизировать с Master.xlsx (колонка Tags: Core 3-6m)	1h	5x	05.02.2026	
AI_Стратегия	Журнал решений (decisions.md)	HIGH	Core	/docs/decisions.md содержит таблицу: | Дата | Решение | Причина | Где зафиксировано | Автор |. Минимум 1 запись при каждом approve стратегического брифа. Текущие записи: 1) 2026-02-05 | Void — главный IDE-оркестратор | снижение context-switching, единый вход для агентов | AI_Стратегия.xlsx (VoidSetup), /docs/strategy.md; 2) 2026-02-05 | Firewall + SSH-keys обязательны для VPS | минимизация риска взлома VPS с PnL | AI_Стратегия.xlsx (WBDataPlatform.Firewall), /docs/security_checklist.md; 3) 2026-02-05 | ContextRAG через публичные HTML-ссылки | синхронный доступ всем LLM, визуально понятная структура | AI_Стратегия.xlsx (ContextRAG), SPACE.xlsx. Проверка: Void-агент на запрос "какие архитектурные решения приняты?" находит эти 3+ записи.	SPACE.xlsx	Создать /docs/decisions.md, внести 3 записи, добавить триггер: после approve → фиксировать решение	30m	3x	05.02.2026	
AI_Стратегия	Eisenhower + Tags Core 3-6m	HIGH	Core	В Master.xlsx/SPACE.xlsx добавлена колонка Tags. Задачи CRITICAL/HIGH, относящиеся к целям 3-6 мес (Void, Smart DB, ContextRAG HTML, TG-бот, Security), помечены "Core 3-6m". Остальные (LuniaFactory v4.0, Capital, дополнительные интеграции) помечены "Backlog" и откладываются. Дорожная карта L2 в AI_Стратегия содержит только "Core 3-6m". Проверка: в Master.xlsx все VoidSetup, WBDataPlatform, ContextRAG, On-prem infra, Dev-RAG, TG-bot имеют Tags="Core 3-6m"; LuniaFactory/Capital имеют "Backlog".	Master.xlsx, SPACE.xlsx	Добавить колонку Tags в Master/SPACE, проставить теги, отфильтровать дорожную карту L2	1h	4x	05.02.2026	
AI_Стратегия	Дорожная карта L2 (актуальная)	HIGH	Core	Markdown-таблица в /docs/strategy.md раздел "Дорожная карта L2": | Проект | Статус | Ссылка Space | Action |. Включает только задачи с Tags="Core 3-6m": VoidSetup, ContextRAG HTML, WBDataPlatform SmartDB, TG-бот, On-prem Docker, Dev-RAG v2, SecurityProd. Каждая строка содержит конкретный Action (что делать дальше). Проверка: таблица содержит 7-10 проектов, все с DoD и Action, нет задач "Backlog".	SPACE.xlsx, Master.xlsx	Собрать дорожную карту L2 из Master/SPACE (фильтр Tags="Core 3-6m"), записать в /docs/strategy.md	1h	5x	05.02.2026	
Space	Проект	Priority	Level	DoD	Ссылка	Action	Effort	ROI	Дата	
VoidSetup	Ollama Serve на Windows Host	CRITICAL	Core	ollama serve запущен на Windows Host (GPU RTX 4080 16GB). Проверки: 1) curl http://localhost:11434 → 200 OK; 2) nvidia-smi показывает VRAM утилизацию при запросе к модели; 3) Void IDE подключён к localhost:11434 через config.json; 4) логи Ollama чистые (нет ошибок CUDA/VRAM). Documented в /docs/ollama_setup.md.	AI_Стратегия.xlsx	Запустить ollama serve, проверить curl/nvidia-smi, настроить Void config.json, задокументировать	30m	10x	05.02.2026	
VoidSetup	qwen2.5-coder-32b (Architect-Local)	CRITICAL	Core	ollama pull qwen2.5-coder:32b-instruct-q4_K_M (~20GB). Architect-Local для сложных задач: DDL, docker-compose, wb-fetcher архитектура. Latency 10-15s на RTX 4080. Проверка: ollama list содержит qwen2.5-coder:32b-instruct-q4_K_M; Void может вызвать модель; тест: "сгенерируй DDL для wborders" → валидный SQL.	AI_Стратегия.xlsx	Pull модели, настроить в Void config как Architect-Local, прогнать тест DDL	1h	10x	05.02.2026	
VoidSetup	qwen2.5-coder-14b (Coder-Fast-Local)	HIGH	Core	ollama pull qwen2.5-coder:14b-instruct-q4_K_M (~8GB). Coder-Fast-Local для быстрых задач: CRUD, endpoints, рефакторинг. Latency ~5s. Проверка: модель в ollama list; Void config содержит Coder-Fast-Local; тест: "сгенерируй FastAPI endpoint /pnl" → валидный код.	AI_Стратегия.xlsx	Pull модели, настроить в Void, тест endpoint	30m	8x	05.02.2026	
VoidSetup	mistral-nemo-12b (Analyst-LongContext)	MEDIUM	Core	ollama pull mistral-nemo:12b-instruct-q4_0 (~7GB). NeMo-LongContext для анализа SQL, JSON, длинных логов. Проверка: модель в списке; тест: "проанализируй этот SQL на n+1 проблемы" → находит проблемы.	AI_Стратегия.xlsx	Pull модели, настроить как Analyst, тест SQL analysis	30m	5x	05.02.2026	
VoidSetup	Cloud-Senior (OpenRouter)	HIGH	Core	OpenRouter API с Claude Sonnet 4 / GPT-4.1 как Cloud-Senior для ревью, PnL-аналитики, сложных промптов. Лимит 2000₽/мес. Проверка: .env содержит OPENROUTER_API_KEY; Void config содержит Cloud-Senior; тест: ревью кода wb-fetcher → находит 0-1 критичную ошибку.	AI_Стратегия.xlsx	Добавить OPENROUTER_API_KEY в .env, настроить Void, тест ревью	30m	7x	05.02.2026	
VoidSetup	System Prompts (4 агента)	CRITICAL	Core	Git-репо /docs/prompts/ содержит 4 файла: ARCHITECT.md (32B), CODER.md (14B), ANALYST.md (NeMo), JUDGE.md (Cloud-Senior). Каждый содержит: 1) роль и зона ответственности; 2) правило "см. /docs/strategy.md, /docs/kb/ как источник контекста, не придумывай с нуля"; 3) запрет PII в логах (переменные → .env); 4) ссылка на Smart DB схему (wborders/wbfinancereports/wbproducts) при работе с WB; 5) формат вывода (markdown/JSON/SQL). Проверка: Void генерирует CRUD todo + wb-fetcher skeleton с корректными импортами по этим промптам.	SPACE.xlsx	Создать /docs/prompts/, написать 4 файла, прогнать тест CRUD+wb-fetcher через Void	2h	10x	05.02.2026	
VoidSetup	Void config.json	CRITICAL	Core	~/.continue/config.json содержит: 1) models.Architect-Local: provider=ollama, model=qwen2.5-coder:32b-instruct-q4_K_M, contextLength=6144, systemMessage="см. /docs/prompts/ARCHITECT.md"; 2) models.Coder-Fast-Local: qwen2.5-coder:14b-instruct-q4_K_M, numCtx=4096, systemMessage="см. /docs/prompts/CODER.md"; 3) models.Analyst: mistral-nemo:12b-instruct-q4_0, systemMessage="см. /docs/prompts/ANALYST.md"; 4) models.Cloud-Senior: provider=openrouter, model=anthropic/claude-sonnet-4, apiKey=$OPENROUTER_API_KEY, systemMessage="см. /docs/prompts/JUDGE.md"; 5) tabAutocomplete: disabled (экономия VRAM); 6) allowAnonymousTelemetry=false. Проверка: Void успешно вызывает Architect-Local для задачи "DDL wborders" → код валидный.	AI_Стратегия.xlsx	Написать config.json, протестировать вызов Architect-Local, сохранить в ~/.continue/	1h	10x	05.02.2026	
VoidSetup	Agentic Flow: CRUD + wb-fetcher + docker-compose	HIGH	Core	Void/Architect-Local + Coder-Fast-Local прогнаны на 3 эталонных задачах: 1) CRUD todos (FastAPI+Postgres, httpx, Pydantic, pytest) → код запускается, тесты зелёные; 2) wb-fetcher skeleton (Python 3.11+, requests, wborders/wbfinancereports upsert в Postgres) → DDL корректен, fetcher делает GET к mock API; 3) docker-compose.yml (n8n, Ollama, Postgres+pgvector, Redis, FastAPI, Cloudflare Tunnel) → docker-compose up -d работает, healthchecks проходят. Время генерации: <5 мин (14B), <10 мин (32B). Cloud-Senior делает ревью, находит 0-1 критичную ошибку. Результаты в /docs/void_benchmarks.md.	AI_Стратегия.xlsx, ContextRAG	Прогнать 3 задачи через Void, записать время/результаты, ревью Cloud-Senior, задокументировать	3h	10x	05.02.2026	
VoidSetup	Void как главный оркестратор (мета-правило)	CRITICAL	Core	Все задачи по разработке (код, миграции, docker-compose, wb-fetcher, промпты, SQL DDL) проходят через Void/Continue + агенты. Исключения: hotfix < 5 строк в production. Проверка: последние 10 коммитов в git /wb-fetcher/, /docker/, /db/ содержат "Generated by Void/Architect-Local" или "Reviewed by Cloud-Senior" в commit message; ни одна задача HIGH/CRITICAL за последние 2 недели не выполнена "вручную в блокноте". Правило зафиксировано в /docs/strategy.md раздел "Правила работы с ИИ".	AI_Стратегия.xlsx, SPACE.xlsx	Прописать правило в /docs/strategy.md, проверить последние 10 коммитов, зафиксировать в decisions.md	30m	10x	05.02.2026	
Space	Проект	Priority	Level	DoD	Ссылка	Action	Effort	ROI	Дата	
WBDataPlatform	VPS Postgres 16 + pgvector prod	CRITICAL	Core	VPS (Aeza/Timeweb/Selectel) 4 vCPU, 8GB RAM, NVMe, Ubuntu 22.04/24.04, Postgres 16 из официального репо. CREATE EXTENSION vector; Схема Smart DB: wborders (id, date, sku, price, qty, cost, profit), wbfinancereports (date, operation_type, amount, penalty, logistics), wbproducts (sku, name, brand, category), wbdocs (id, content, embedding vector(768)) с индексом IVFFlat. mv_daily_pnl создана, функция get_daily_pnl(date_from, date_to) возвращает JSON с revenue/costs/PnL. pg_dump в cron (03:00), последний dump валиден. Firewall UFW настроен (см. SecurityProd). Тест: SELECT * FROM mv_daily_pnl WHERE date='2026-02-05' → корректные суммы (сверка с Excel). Documented в /docs/smart_db_schema.md.	AI_Стратегия.xlsx, SecurityProd	Арендовать VPS, установить Postgres 16+pgvector, создать схему Smart DB, создать mv_daily_pnl + get_daily_pnl, настроить backup, тест SELECT, задокументировать	6h	10x	05.02.2026	
WBDataPlatform	wborders таблица	HIGH	Core	CREATE TABLE wborders (id serial primary key, date date not null, sku varchar(50), price numeric(10,2), qty int, cost numeric(10,2), profit numeric(10,2), created_at timestamp default now()); CREATE INDEX idx_wborders_date_sku ON wborders(date, sku); Тест: INSERT sample data → SELECT работает, индекс используется (EXPLAIN). SQL в git /db/migrations/001_wborders.sql.	Master.xlsx	Написать DDL через Void/Architect-Local, задеплоить на VPS, тест INSERT/SELECT, commit в git	1h	8x	05.02.2026	
WBDataPlatform	wbfinancereports таблица	HIGH	Core	CREATE TABLE wbfinancereports (id serial primary key, date date not null, sku varchar(50), operation_type varchar(50), amount numeric(10,2), penalty numeric(10,2), logistics numeric(10,2), created_at timestamp); CREATE INDEX idx_wbfinancereports_date ON wbfinancereports(date); Тест: JOIN с wborders по date, sku → данные связываются корректно. SQL в git /db/migrations/002_wbfinancereports.sql.	Master.xlsx	DDL через Void, деплой, тест JOIN, commit	1h	8x	05.02.2026	
WBDataPlatform	wbproducts таблица	MEDIUM	Core	CREATE TABLE wbproducts (sku varchar(50) primary key, name text, brand varchar(100), category varchar(100), created_at timestamp); Тест: JOIN с wborders → получение названия товара по SKU. SQL в git /db/migrations/003_wbproducts.sql.	Master.xlsx	DDL через Void, деплой, тест JOIN, commit	30m	5x	05.02.2026	
WBDataPlatform	mv_daily_pnl витрина	CRITICAL	Core	CREATE MATERIALIZED VIEW mv_daily_pnl AS SELECT date, SUM(price * qty) as revenue, SUM(cost * qty) as cogs, SUM(penalty) as penalties, SUM(logistics) as logistics, (SUM(price * qty) - SUM(cost * qty) - SUM(penalty) - SUM(logistics)) as pnl FROM wborders LEFT JOIN wbfinancereports USING (date, sku) GROUP BY date; Функция: CREATE FUNCTION get_daily_pnl(date_from DATE, date_to DATE) RETURNS TABLE(...) AS SELECT∗FROMmvdailypnlWHEREdateBETWEENdatefromANDdatetoSELECT * FROM mv_daily_pnl WHERE date BETWEEN date_from AND date_toSELECT∗FROMmvdailypnlWHEREdateBETWEENdatefromANDdateto; REFRESH в cron (04:00 ежедневно после wb-fetcher). Тест: SELECT * FROM get_daily_pnl('2026-01-01', '2026-01-31') → 31 строка, PnL совпадает с Excel (±1%). Cloud-Senior валидирует SQL (нет n+1, индексы на date/sku). SQL в git /db/migrations/004_mv_daily_pnl.sql, documented в /docs/pnl_calculation.md.	AI_Стратегия.xlsx	SQL через Void/Architect-Local, ревью Cloud-Senior, деплой, настроить cron REFRESH, тест сверки с Excel, задокументировать	3h	10x	05.02.2026	
WBDataPlatform	wb-fetcher ETL	HIGH	Core	Python 3.11+ проект: main.py (entry), fetchers/ (orders_fetcher.py, finances_fetcher.py, stocks_fetcher.py), db/ (models.py Pydantic, upsert.py), config.py (WB_API_KEY из .env, DB connection). DDL сгенерирован Architect-Local. Fetcher делает GET к WB API (реальный/mock), парсит JSON, upsert в Postgres (ON CONFLICT DO UPDATE). Cron/systemd: каждые 6 часов. Логи /var/log/wb-fetcher.log, ротация. Тест: запуск вручную → wborders появляются новые строки, повторный запуск → дубликатов нет. Код в git /wb-fetcher/, documented в /docs/wb_fetcher_setup.md.	AI_Стратегия.xlsx, VoidSetup	Сгенерировать архитектуру через Void/Architect-Local, написать код Coder-Fast-Local, тест upsert, настроить cron, задокументировать	5h	10x	05.02.2026	
WBDataPlatform	WB API endpoints (заметка)	HIGH	-	WB API endpoints (из Apps Script / личного кабинета): orders, sales, stocks, finances. Endpoint TODO в /docs/wb_api_endpoints.md. Проверка: список endpoints актуален, токены/ключи в .env, не в коде.	Master.xlsx	Собрать список endpoints из Apps Script, задокументировать в /docs/wb_api_endpoints.md, переменные в .env	1h	5x	05.02.2026	
WBDataPlatform	FastAPI /pnl endpoint	MEDIUM	Core	FastAPI main.py: GET /pnl?date_from=YYYY-MM-DD&date_to=YYYY-MM-DD → SELECT * FROM get_daily_pnl(date_from, date_to) → JSON response. Healthcheck GET /health → {"status": "ok"}. Docker-контейнер в docker-compose (см. On-prem infra). Тест: curl http://localhost:8000/pnl?date_from=2026-01-01&date_to=2026-01-31 → JSON 31 строк, данные корректны. Код в git /backend/, documented в /docs/fastapi_setup.md.	On-prem infra	Сгенерировать FastAPI через Void/Coder-Fast-Local, добавить в docker-compose, тест curl, задокументировать	2h	7x	05.02.2026	
WBDataPlatform	ExcelBI подключение к Postgres	MEDIUM	-	Excel/Power BI подключается к Postgres/Supabase через Power Query или ODBC. Тест: SELECT FROM mv_daily_pnl → данные в Excel. Инструкция в /docs/excel_bi_setup.md.	Master.xlsx	Настроить Power Query / ODBC, протестировать подключение, задокументировать	1h	5x	05.02.2026	
Space	Проект	Priority	Level	DoD	Ссылка	Action	Effort	ROI	Дата	
On-prem-infra	docker-compose.yml AI stack	CRITICAL	Core	docker-compose.yml содержит сервисы: n8n (n8nio/n8n:latest, 5678, env: NODE_ENV=production, EXECUTIONS_MODE=queue, QUEUE_BULL_REDIS_HOST=redis, PRUNE=true, WEBHOOK_URL из .env, volumes: ./.n8n_data), ollama (ghcr.io/ollama/ollama, 11434, deploy.resources.reservations.devices GPU, volumes: ./.ollama), postgres (pgvector/pgvector:pg16, 5432, POSTGRES_USER/PASSWORD/DB из .env, volumes: ./.pgdata), redis (redis:alpine, 6379, volumes: ./.redis-data), fastapi (build: ./backend, 8000, depends_on: postgres, healthcheck), cloudflared (cloudflare/cloudflared, TUNNEL_TOKEN из .env). Network: ai-net. Тест: docker-compose up -d → все сервисы healthy, curl http://localhost:5678 → n8n UI, curl http://localhost:11434 → Ollama API, curl http://localhost:8000/health → FastAPI OK, psql → Postgres доступен, cloudflared tunnel → https://n8n.yourdomain.com работает. Файл в git /docker/docker-compose.yml, .env.example, documented в /docs/docker_setup.md.	AI_Стратегия.xlsx	Сгенерировать docker-compose через Void/Architect-Local, создать .env.example, тест docker-compose up, задокументировать	4h	10x	05.02.2026	
On-prem-infra	.env секреты	CRITICAL	Core	.env содержит (примеры в .env.example, реальные в .gitignore): TUNNEL_TOKEN=eyJh..., POSTGRES_USER=wbuser, POSTGRES_PASSWORD=<openssl rand -base64 32>, POSTGRES_DB=wbdb, N8N_USER=admin, N8N_PASS=<сильный пароль>, WEBHOOK_URL=https://n8n.yourdomain.com, OLLAMA_MAX_LOADED_MODELS=1, OLLAMA_FLASH_ATTENTION=1, WB_API_KEY=<из личного кабинета WB>, OPENROUTER_API_KEY=<для Cloud-Senior>, ALLOWED_USER_IDS=<Telegram user IDs>. Проверка: docker-compose читает переменные, сервисы стартуют; git status → .env в .gitignore, не коммитится; grep -r "POSTGRES_PASSWORD" logs/ → пусто. Documented в /docs/security_checklist.md.	SecurityProd	Создать .env.example, сгенерировать пароли через openssl, добавить .env в .gitignore, тест grep логов, задокументировать	1h	10x	05.02.2026	
On-prem-infra	WSL2 Ubuntu + Docker Desktop	HIGH	Core	WSL2 Ubuntu 24.04 установлен, Docker Desktop интегрирован с WSL Backend. Проверка: wsl -l -v → Ubuntu Running; docker ps из WSL → работает; .wslconfig содержит memory=24GB, processors=8. Documented в /docs/wsl_setup.md.	AI_Стратегия.xlsx	Установить WSL2, настроить .wslconfig, установить Docker Desktop, тест docker ps, задокументировать	2h	8x	05.02.2026	
On-prem-infra	UAT E2E latency + VRAM	HIGH	Core	UAT E2E сценарий: 1) TG /pnl → < 3s (SELECT mv_daily_pnl); 2) "какой SKU самый убыточный?" → RAG v2 (embed → pgvector → LLM → Judge) < 10s; 3) nvidia-smi во время выполнения: VRAM < 16GB (gpt-oss20b + nomic-embed одновременно); 4) стресс: 100 параллельных /pnl → avg latency < 5s, 0 ошибок, Redis queue не переполняется; 5) docker restart postgres → n8n/fastapi автоматом переподключаются (< 30s), запросы работают; 6) логи чистые (нет CUDA errors, OOM, connection refused). Результаты в /docs/uat_2026-02-DD.md. Pass criteria: все 6 green, 0 critical bugs. При fail → фикс, повтор UAT.	AI_Стратегия.xlsx, Dev-RAG	Запустить UAT сценарии, записать метрики (latency, VRAM, errors), задокументировать результаты, при fail → фикс	4h	10x	05.02.2026	
Space	Проект	Priority	Level	DoD	Ссылка	Action	Effort	ROI	Дата	
Dev-RAG	wbdocs таблица + pgvector	HIGH	Core	CREATE TABLE wbdocs (id serial primary key, content text, embedding vector(768), created_at timestamp); Для < 10k строк: CREATE INDEX idx_wbdocs_embedding ON wbdocs USING ivfflat (embedding vector_cosine_ops) WITH (lists=100); для > 10k: HNSW (CREATE INDEX ... USING hnsw (embedding vector_cosine_ops) WITH (m=16, ef_construction=64)). Seed: 100-500 документов (статьи WB, FAQ, /docs/strategy.md, примеры запросов PnL). Тест similarity search: SELECT content FROM wbdocs ORDER BY embedding <=> '[0.1, 0.2, ..., 0.768]' LIMIT 5 → время < 100ms (для 10k строк). Benchmark: 100 запросов, avg latency < 150ms, recall@5 > 80%. SQL в git /db/migrations/005_wbdocs_rag.sql, seed в /db/seeds/wbdocs.sql, documented в /docs/rag_db_setup.md.	WBDataPlatform	DDL через Void/Architect-Local, создать индекс IVFFlat/HNSW, seed данных, тест similarity search, benchmark, задокументировать	3h	8x	05.02.2026	
Dev-RAG	n8n workflow RAG v2 + Judge	CRITICAL	Core	n8n workflow "RAG v2 + Judge": 1) Webhook (POST /rag, body: {query: "..."}); 2) Ollama Embed (nomic-embed-text, 768-dim) → vector; 3) Postgres: SELECT content FROM wbdocs ORDER BY embedding <=> $vector LIMIT 5; 4) Transform: join context; 5) Ollama Chat (gpt-oss20b, prompt: "context: ... query: ... answer:"); 6) LLM-Judge (mistral-nemo или Cloud-Senior): "оцени relevance/correctness/clarity 1-10, JSON: {score, reason}"; 7) If score < 7 → Retry (max 2, изменить промпт), else → Respond; 8) логи в Postgres (rag_logs: query, context, answer, score, latency_ms). Тест: POST /rag {"query": "какой PnL за январь?"} → answer корректен (содержит цифры из mv_daily_pnl), score ≥7, latency < 10s, логи записаны. Workflow JSON в git /n8n/workflows/rag_v2.json, documented в /docs/rag_v2_setup.md.	AI_Стратегия.xlsx, On-prem-infra	Создать n8n workflow (8 nodes), настроить Ollama Embed/Chat, Postgres, LLM-Judge, тест query PnL, задокументировать	5h	10x	05.02.2026	
Dev-RAG	nomic-embed-text embedding model	HIGH	Core	ollama pull nomic-embed-text (768-dim). Проверка: ollama list содержит nomic-embed-text; n8n Ollama Embed node работает; тест: embed "какой PnL" → vector 768 размерности.	VoidSetup	Pull модели, тест через n8n Ollama Embed node, задокументировать в /docs/rag_v2_setup.md	30m	7x	05.02.2026	
Dev-RAG	LLM-as-Judge (mistral-nemo или Cloud-Senior)	HIGH	Core	LLM-Judge node в n8n workflow: prompt "Оцени ответ по критериям: relevance (релевантность контексту), correctness (фактическая правильность), clarity (ясность). Выведи JSON: {score: 1-10, reason: 'объяснение'}". Модель: mistral-nemo (локально) или Cloud-Senior (OpenRouter). Тест: answer "PnL за январь: 123k" → score ≥7 (если цифры совпадают с mv_daily_pnl), score < 7 (если галлюцинация). Задокументировано в /docs/rag_v2_setup.md раздел LLM-Judge.	On-prem-infra	Добавить LLM-Judge node в workflow, прописать промпт, тест score, задокументировать	2h	8x	05.02.2026	
Dev-RAG	RAG качество: recall@5, latency	MEDIUM	Core	Benchmark RAG v2: 100 запросов (ground truth: эталонные ответы из /docs/, mv_daily_pnl). Метрики: recall@5 > 80% (из 5 найденных документов хотя бы 1 релевантен), avg latency < 150ms (pgvector search), end-to-end latency < 10s (webhook → answer). Результаты в /docs/rag_benchmark_2026-02-DD.md. При fail → оптимизация индекса (HNSW) или увеличение LIMIT.	AI_Стратегия.xlsx	Подготовить 100 эталонных запросов, запустить benchmark, записать метрики, задокументировать	3h	7x	05.02.2026	
Space	Проект	Priority	Level	DoD	Ссылка	Action	Effort	ROI	Дата	
SecurityProd	Firewall UFW на VPS	CRITICAL	Core	UFW active, правила: ufw allow 22/tcp (только с IP whitelist: твой VPN/дом), ufw allow 5432/tcp (только localhost или VPN IP), ufw default deny incoming, ufw default allow outgoing. Тест: ufw status → active, 22 и 5432 разрешены только для whitelist IP, остальные deny; попытка SSH с внешнего IP → connection refused, с whitelist IP → success. Documented в /docs/security_checklist.md раздел Firewall.	WBDataPlatform	Настроить UFW на VPS, добавить правила, whitelist IP, тест SSH, задокументировать	1h	10x	05.02.2026	
SecurityProd	SSH-ключи (отключение паролей)	CRITICAL	Core	sshd_config: PasswordAuthentication no, PubkeyAuthentication yes, PermitRootLogin no. SSH-ключ установлен (ssh-copy-id user@vps), пароль отключён. Тест: ssh root@vps → "Permission denied", ssh user@vps -i ~/.ssh/id_rsa → success. Documented в /docs/security_checklist.md раздел SSH.	WBDataPlatform	Сгенерировать SSH-ключ, установить на VPS, отключить пароли в sshd_config, тест SSH, задокументировать	1h	10x	05.02.2026	
SecurityProd	Postgres pg_hba.conf (IP whitelist)	CRITICAL	Core	postgresql.conf: listen_addresses='localhost,10.x.x.x' (localhost + VPN IP). pg_hba.conf содержит: local all postgres peer, host wbdb wbuser 10.x.x.x/32 scram-sha-256, host all all 0.0.0.0/0 reject. Тест: psql из внешней сети → "connection refused", psql из VPN IP → success, psql из localhost → success. Documented в /docs/security_checklist.md раздел Postgres.	WBDataPlatform	Отредактировать postgresql.conf и pg_hba.conf, перезапустить Postgres, тест psql из разных IP, задокументировать	1h	10x	05.02.2026	
SecurityProd	backup.sh + cron	HIGH	Core	backup.sh содержит: pg_dump -U wbuser -d wbdb -F c -f /backups/wbdb_$(date +\%Y\%m\%d).dump, использует .pgpass (пароль не в скрипте). Cron: 0 3 * * * /path/to/backup.sh (ежедневно 03:00). Тест: запуск backup.sh → файл /backups/wbdb_YYYYMMDD.dump создан, pg_restore --list → валиден, последний backup < 24 часов. Скрипт в git /scripts/backup.sh, documented в /docs/security_checklist.md раздел Backup.	WBDataPlatform	Написать backup.sh, создать .pgpass, настроить cron, тест backup + restore, задокументировать	2h	10x	05.02.2026	
SecurityProd	.env в .gitignore (no secrets in git)	CRITICAL	Core	.gitignore содержит .env, .env.local, *.env. Git history проверен: git log --all --full-history -- .env → пусто (никогда не коммитился). Тест: git status после изменения .env → "nothing to commit" (ignored), grep -r "POSTGRES_PASSWORD" .git/ → пусто. Documented в /docs/security_checklist.md раздел Secrets.	On-prem-infra	Добавить .env в .gitignore, проверить git history, тест git status, задокументировать	30m	10x	05.02.2026	
SecurityProd	n8n BasicAuth + PRUNE + Redis queue	HIGH	Core	n8n docker-compose env: N8N_BASIC_AUTH_ACTIVE=true, N8N_BASIC_AUTH_USER=$N8N_USER, N8N_BASIC_AUTH_PASSWORD=$N8N_PASS (из .env, пароль > 16 символов), EXECUTIONS_PRUNE=true, EXECUTIONS_DATA_MAX_AGE=168 (7 дней), EXECUTIONS_MODE=queue, QUEUE_BULL_REDIS_HOST=redis. Тест: открыть https://n8n.yourdomain.com → запрос логина/пароля, без логина → 401; старые executions удаляются через 7 дней. Documented в /docs/docker_setup.md раздел n8n Security.	On-prem-infra	Добавить env-переменные в docker-compose, создать сильный пароль в .env, тест BasicAuth, задокументировать	1h	8x	05.02.2026	
SecurityProd	Redis bind 127.0.0.1 (no external access)	HIGH	Core	Redis docker-compose: command: redis-server --bind 127.0.0.1 --requirepass $REDIS_PASSWORD (из .env). Тест: redis-cli -h vps_external_ip → connection refused, redis-cli -h 127.0.0.1 -a $REDIS_PASSWORD (из контейнера) → success. Documented в /docs/docker_setup.md раздел Redis Security.	On-prem-infra	Добавить command в docker-compose, REDIS_PASSWORD в .env, тест redis-cli, задокументировать	30m	7x	05.02.2026	
SecurityProd	Cloudflare Tunnel (no open ports)	HIGH	Core	Cloudflare Tunnel вместо открытия портов 5678 (n8n), 8000 (FastAPI) наружу. cloudflared docker-compose: TUNNEL_TOKEN из .env. Тест: ufw status → порты 5678, 8000 не открыты; https://n8n.yourdomain.com → работает через tunnel; https://api.yourdomain.com → FastAPI. Documented в /docs/docker_setup.md раздел Cloudflare Tunnel.	On-prem-infra	Создать Cloudflare Tunnel, получить TUNNEL_TOKEN, добавить cloudflared в docker-compose, тест HTTPS, задокументировать	2h	8x	05.02.2026	
SecurityProd	Security Checklist (итоговый документ)	CRITICAL	Core	/docs/security_checklist.md содержит разделы: 1) VPS: Firewall UFW (правила, тест), SSH-ключи (отключение паролей, тест), Postgres pg_hba.conf (IP whitelist, тест), backup/restore (cron, валидация dump), обновления ОС (unattended-upgrades); 2) Docker: .env в .gitignore (тест grep), n8n BasicAuth (пароль > 16, тест 401), Redis bind 127.0.0.1 (тест redis-cli), Cloudflare Tunnel (no open ports); 3) Инцидент: при подозрении на взлом → ротация всех паролей (.env), проверка логов (auth.log, n8n, postgres), откат до last known good backup, пост-мортем в decisions.md. Каждый пункт с командой + expected output. Проверка: при деплое VPS/Docker → прогнать все тесты, результаты в /docs/security_audit_2026-02-DD.md.	AI_Стратегия.xlsx	Написать /docs/security_checklist.md (все разделы), протестировать каждый пункт, задокументировать audit	3h	10x	05.02.2026	
Space	Проект	Priority	Level	DoD	Ссылка	Action	Effort	ROI	Дата	Tags
LuniaFactory	LuniaFactory v4.0 (ComfyUI + Flux.2 + WB)	MEDIUM	Backlog	ComfyUI 0.10.0 portable, Flux.2 Klein 9B FP8, SAM2, GroundingDINO, workflows WBColorEdit.json + WBBeddingMVP.json, batch 7x 900x1200, ROI 0.1-0.5₽/фото. Hardware: RTX 4080 Super 16GB, 48GB RAM. Проверка: workflow генерирует 7 фото за batch < 5 мин, цвет B48685 корректно применяется, watermark opacity 0.06-0.08. Detailed DoD в отдельной таблице LuniaFactory.xlsx (не в Core 3-6m).	Master.xlsx	Отложено до завершения Core 3-6m (Void, Smart DB, PnL, Security). После — вернуться к детальному плану LuniaFactory	20h+	10x	TBD	Backlog
